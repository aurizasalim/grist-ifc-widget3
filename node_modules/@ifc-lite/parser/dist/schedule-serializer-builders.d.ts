/**
 * STEP-line builders for `schedule-serializer.ts` — the low-level string
 * encoding helpers (`escStr`, `optStr`, `optEnum`, `optBool`, `optReal`,
 * `ownerRef`, `refList`, `ensureGlobalId`) and the per-entity builders that
 * turn one `WorkScheduleInfo` / `ScheduleTaskInfo` record into its
 * `#N=IFC...(...);` line (`buildWorkControl`, `buildTaskTime`, `buildTask`,
 * plus the small predicates/resolvers those two lean on).
 *
 * `schedule-serializer.ts` owns the orchestration — which entities to emit,
 * in what order, and how their cross-references (`IFCRELNESTS`,
 * `IFCRELASSIGNSTOCONTROL`, `IFCRELASSIGNSTOPROCESS`, `IFCRELSEQUENCE`) wire
 * together — and calls into this module for "how does one entity's own
 * attribute line get written". Same split as `schedule-types.ts` on the
 * read side: single-entity encoding kept apart from cross-entity wiring.
 */
import type { ScheduleTaskInfo, WorkScheduleInfo } from './schedule-extractor.js';
export declare function escStr(s: string): string;
export declare function optStr(v: string | undefined | null): string;
export declare function optEnum(v: string | undefined | null): string;
export declare function ownerRef(ownerHistoryId: number | undefined): string;
export declare function refList(ids: number[]): string;
export declare function ensureGlobalId(existing: string | undefined, seed: string): string;
export declare function buildWorkControl(id: number, ws: WorkScheduleInfo, owner: string): string;
export declare function taskHasTimeData(task: ScheduleTaskInfo): boolean;
export declare function buildTaskTime(id: number, task: ScheduleTaskInfo): string;
export declare function buildTask(id: number, task: ScheduleTaskInfo, owner: string, taskTimeId: number | undefined): string;
export declare function resolveProductIds(task: ScheduleTaskInfo, resolver?: (gid: string) => number | undefined): number[];
//# sourceMappingURL=schedule-serializer-builders.d.ts.map