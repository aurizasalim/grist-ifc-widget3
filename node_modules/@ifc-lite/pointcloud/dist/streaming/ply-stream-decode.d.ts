import type { DecodedPointChunk, PointCloudBBox, PointNormalState } from '../types.js';
import type { PlyHeader, PlyVertexLayout } from '../formats/ply.js';
export declare const PLY_STREAM_READ_BYTES: number;
export interface PlyStreamInstrumentation {
    /** Test-only observation of output channel capacity; no source-count buffer is allocated. */
    onOutputAllocation?: (pointCapacity: number) => void;
}
export declare function scanPlyBounds(blob: Blob, header: PlyHeader, layout: PlyVertexLayout, origin: readonly [number, number, number] | undefined, signal?: AbortSignal): Promise<{
    bbox: PointCloudBBox;
    normalState: PointNormalState;
    floatColorsUseByteRange: boolean;
}>;
export declare class PlyChunkReader {
    private readonly blob;
    private readonly header;
    private readonly layout;
    private readonly stride;
    private readonly origin;
    private readonly normalState;
    private readonly floatColorsUseByteRange;
    private readonly instrumentation?;
    private sourceRow;
    private readonly ascii;
    private readonly channels;
    private readonly variableBinary;
    constructor(blob: Blob, header: PlyHeader, layout: PlyVertexLayout, stride: number, origin: readonly [number, number, number] | undefined, normalState: PointNormalState, floatColorsUseByteRange: boolean, instrumentation?: PlyStreamInstrumentation | undefined);
    next(maxPoints: number, signal?: AbortSignal): Promise<DecodedPointChunk | null>;
    private readBinary;
    private readVariableBinary;
    private readAscii;
}
//# sourceMappingURL=ply-stream-decode.d.ts.map