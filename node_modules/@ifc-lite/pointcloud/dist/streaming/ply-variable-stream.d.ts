import type { PlyPropertyOrderEntry } from '../formats/ply.js';
/** Bounded sequential traversal for binary PLY rows containing variable-length lists. */
export declare class BinaryPlyRowCursor {
    private readonly blob;
    private readonly littleEndian;
    private offset;
    private window;
    private windowOffset;
    constructor(blob: Blob, bodyOffset: number, littleEndian: boolean);
    row(order: readonly PlyPropertyOrderEntry[], row: number, signal?: AbortSignal): Promise<Map<string, number>>;
    private take;
    private skip;
    private refill;
}
//# sourceMappingURL=ply-variable-stream.d.ts.map