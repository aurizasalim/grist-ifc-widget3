/**
 * PCD (PCL) streaming source — whole-file decode, single chunk.
 *
 * Like PLY, PCD isn't an octree format. The decoder lives in
 * `formats/pcd.ts` and handles ASCII / binary / binary_compressed
 * (LZF). For files past the 25M-point soft cap, `streamPointCloud`
 * applies stride downsampling on the way out.
 */
import type { DecodedPointChunk } from '../types.js';
import type { DownsampleHint, PointSourceInfo, StreamingPointSource } from './types.js';
export declare class PcdStreamingSource implements StreamingPointSource {
    private blob;
    private downsample;
    private label?;
    private chunk;
    private served;
    /** See `decodePcd`'s `originOffset` param (extends #1804's LAS/LAZ
     *  pattern). */
    private originOffset?;
    constructor(blob: Blob, options?: {
        label?: string;
        downsample?: DownsampleHint;
        originOffset?: readonly [number, number, number];
    });
    open(signal?: AbortSignal): Promise<PointSourceInfo>;
    next(maxPoints: number, signal?: AbortSignal): Promise<DecodedPointChunk | null>;
    close(): void;
}
//# sourceMappingURL=pcd-source.d.ts.map