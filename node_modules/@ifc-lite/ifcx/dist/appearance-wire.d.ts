import type { IfcxSchema } from './types.js';
export declare const IFCX_APPEARANCE = "ifclite::appearance::v1";
export declare const IFCX_IMAGE = "ifclite::image::v1";
export interface IfcxEncodedImage {
    mimeType: 'image/png' | 'image/jpeg';
    bytes: Uint8Array;
}
export interface IfcxPixels {
    /** Optional original source bytes, retained without decoding or recompression. */
    original?: IfcxEncodedImage;
    width: number;
    height: number;
    rgba: Uint8Array;
    repeatS: boolean;
    repeatT: boolean;
}
export interface IfcxAppearance {
    image: {
        ref: string;
    };
    uvs: number[];
    repeatS: boolean;
    repeatT: boolean;
}
export interface IfcxImage {
    original?: {
        mimeType: 'image/png' | 'image/jpeg';
        data: string;
    };
    width: number;
    height: number;
    /** Straight-alpha, top-down RGBA8. Base64 is transport, not a color conversion. */
    rgba: string;
}
/** Inline declarations: readers without this extension still see standard geometry. */
export declare const IFCX_APPEARANCE_SCHEMAS: Record<string, IfcxSchema>;
export declare function encodeIfcxImage(pixels: IfcxPixels): {
    key: string;
    value: IfcxImage;
};
/** One decoder per file, shared across occurrences, with an aggregate allocation budget. */
export declare function createIfcxImageDecoder(): (path: string, value: unknown) => Omit<IfcxPixels, 'repeatS' | 'repeatT'>;
export declare function decodeIfcxAppearance(value: unknown, vertexCount: number): IfcxAppearance;
//# sourceMappingURL=appearance-wire.d.ts.map