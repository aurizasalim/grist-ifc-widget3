import type { MeshData } from '@ifc-lite/geometry';
/** Resolve a rendered triangle to its canonical evaluated-surface ordinal.
 * Returns undefined when topology provenance is absent or ambiguous. */
export declare function appearanceSourceTriangle(mesh: MeshData, triangleIndex: number): number | undefined;
/** Bind canonical final triangle corners, expanding welded vertices only for
 * appearance. Geometry coordinates and triangle order remain exactly unchanged. */
export declare function expandAppearanceCorners(mesh: MeshData, canonicalSourceIndices: ArrayLike<number>, canonicalCornerUvs: ArrayLike<number>, canonicalTargetIndices: ArrayLike<number>, canonicalTargetCornerNormals: ArrayLike<number>, canonicalTargetVertexCount: number): MeshData;
/** Exact triangle-corner equivalence for history validation. Normal changes are
 * opt-in when applying canonical appearance shading; positions stay exact. */
export declare function equivalentAppearanceGeometry(a: MeshData, b: MeshData, options?: {
    allowNormalChanges?: boolean;
}): boolean;
//# sourceMappingURL=appearance-uvs.d.ts.map