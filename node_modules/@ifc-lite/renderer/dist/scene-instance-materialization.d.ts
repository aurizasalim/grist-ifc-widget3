import type { MeshData } from '@ifc-lite/geometry';
interface Occurrence {
    templateIndex: number;
    byteOffset: number;
    originalColor: [number, number, number, number];
    itemId?: number;
}
interface Template {
    modelIndex: number;
    positions: Float32Array;
    normals: Float32Array;
    indices: Uint32Array;
    instanceData: ArrayBuffer;
}
/** CPU display expansion: transformed f32 coordinates are approximate, while template topology remains canonical. */
export declare function materializeInstances(expressId: number, occ: readonly Occurrence[], templates: readonly (Template | undefined)[]): MeshData[] | undefined;
export {};
//# sourceMappingURL=scene-instance-materialization.d.ts.map