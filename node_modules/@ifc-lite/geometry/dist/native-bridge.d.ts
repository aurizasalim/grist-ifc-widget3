/**
 * Native Bridge Implementation
 *
 * Uses Tauri commands for geometry processing in desktop apps.
 * Provides native Rust performance with multi-threading support.
 */
import type { IPlatformBridge, GeometryProcessingResult, GeometryStats, StreamingOptions } from './platform-bridge.js';
/**
 * Native Tauri bridge for desktop apps
 *
 * This uses Tauri's invoke() to call native Rust commands that use
 * ifc-lite-core and ifc-lite-geometry directly (no WASM overhead).
 */
export declare class NativeBridge implements IPlatformBridge {
    private initialized;
    private invoke;
    private listen;
    init(): Promise<void>;
    isInitialized(): boolean;
    private toNativeBuffer;
    private drainDeferredBatches;
    private processEventDrivenNativeStream;
    processGeometry(content: string | Uint8Array): Promise<GeometryProcessingResult>;
    processGeometryPath(path: string): Promise<GeometryProcessingResult>;
    processGeometryStreaming(content: string | Uint8Array, options: StreamingOptions): Promise<GeometryStats>;
    processGeometryStreamingPath(path: string, options: StreamingOptions, cacheKey?: string): Promise<GeometryStats>;
    processGeometryStreamingCache(cacheKey: string, options: StreamingOptions): Promise<GeometryStats>;
    private processPackedShardPathStream;
    getApi(): null;
}
//# sourceMappingURL=native-bridge.d.ts.map