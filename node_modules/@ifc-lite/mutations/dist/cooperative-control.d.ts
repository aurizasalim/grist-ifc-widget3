import type { EntityPreparationOptions } from './cooperative-operation-types.js';
export declare class CooperativeControl {
    private work;
    private bytes;
    private sliceWork;
    private deadline;
    private readonly maxWork;
    private readonly maxBytes;
    private readonly sliceMs;
    private readonly signal;
    private readonly yieldTask;
    constructor(options: EntityPreparationOptions);
    checkAbort(): void;
    step(bytes?: number): boolean;
    pause(): Promise<void>;
}
//# sourceMappingURL=cooperative-control.d.ts.map